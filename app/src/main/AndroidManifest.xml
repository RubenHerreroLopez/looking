<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rmpsoft.looking">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".ChatListActivity"></activity>
        <activity
            android:name=".ChatActivity" />
            //android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.User_Home" />
        <activity android:name=".activitys.User_Register" />
        <activity android:name=".activitys.User_EditPerfil" />
        <activity android:name=".activitys.Team_Register" />
        <activity android:name=".activitys.Team_PutAdvice" />
        <activity android:name=".activitys.Team_Home" />
        <activity android:name=".activitys.Team_EditPerfil" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>